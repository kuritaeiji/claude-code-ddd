{
  "permissions": {
    "allow": [
      "Read(**)",
      "Write(**)",
      "Edit(**)",
      "Grep(**)",
      "Glob(**)",
      "Bash(date *)",
      "Bash(mkdir *)",
      "Bash(find *)",
      "Bash(grep *)",
      "Bash(go test *)",
      "Agent(*)"
    ]
  },
  "enableAllProjectMcpServers": true,
  "enabledMcpjsonServers": [
    "playwright"
  ],
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -q '\\.md$' && npx prettier --write \"$f\"; } 2>/dev/null || true"
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -q '\\.go$' && gofmt -w \"$f\"; } 2>/dev/null || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); active=$(echo \"$input\" | jq -r '.stop_hook_active // false'); if [ \"$active\" = \"true\" ]; then exit 0; fi; result=$(cd backend && $HOME/.goenv/shims/golangci-lint run ./... 2>&1); rc=$?; if [ $rc -ne 0 ] && [ $rc -ne 5 ]; then jq -cn --arg m \"$result\" '{\"decision\":\"block\",\"reason\":(\"golangci-lint failed:\\n\"+$m+\"\\n\\nFix the lint issues before stopping.\")}'; else exit 0; fi",
            "timeout": 120,
            "statusMessage": "golangci-lint 実行中..."
          }
        ]
      }
    ]
  }
}
